
# Obsidian Automations

Агенты, которые помогают собирать/обрабатывать информацию в Obsidian.

Созданы, чтобы работать локально на моем персональном macbook (m1 pro, 16Gb).

---

## 📑 Содержание

1. [🎙️ Записатель iOS → Obsidian](#-audio-journal-transcriber-ios--obsidian)
2. [🧠 Мудрый Бро](#-мудрый-бро---анализатор-дневниковых-записей)

---

# 🎙️ Audio Journal Transcriber (iOS → Obsidian)

Автоматический конвейер для обработки голосовых заметок. Превращает аудио с диктофона iPhone в отформатированные записи в Obsidian, используя локальный ИИ (Whisper + LLM).

## ✨ Возможности

*   **Приватность:** Все работает локально на Mac (без облачных API).
*   **Синхронизация:** Записываете в стандартном диктофоне iPhone — через iCloud появляется на Mac.
*   **Умная транскрибация:** Использует `faster-whisper` для точного распознавания речи.
*   **AI-анализ (Llama/Qwen):**
    *   Разделяет записи на **Дневник** и **Заметки**.
    *   Структурирует дневник (Сон, Здоровье, Успехи, Оценка дня).
    *   Исправляет пунктуацию и разбивает текст на абзацы.
    *   **Умные даты:** Понимает, что запись в 2 часа ночи про "сегодня" относится к вчерашнему дню.
*   **Архивация:** Сохраняет аудиофайлы в долгосрочное хранилище.
*   **Obsidian:** Создает красивые Markdown-файлы со ссылками на аудио.
*   **Git:** Автоматически делает commit & push изменений в репозиторий Obsidian.

---

## ⚙️ Требования

*   **Hardware:** Mac с чипом Apple Silicon рекомендуется (для быстрой работы нейросетей).
*   **OS:** macOS (Sonoma/Sequoia) (из-за особенностей синхронизации icloud).
*   **iPhone:** С включенной синхронизацией Диктофона в iCloud.
*   **Software:**
    *   Python 3.10+
    *   [Ollama](https://ollama.com/) (для запуска LLM).
    *   Git.

---

## 🚀 Установка и Настройка

### 1. Установка Ollama и Модели
Скрипт использует локальную LLM для анализа текста.
1. Скачайте и установите [Ollama](https://ollama.com/).
2. В терминале скачайте модель (рекомендуется `qwen3` для русского языка):
   ```bash
   ollama pull qwen3:8b
   ```
   *Если вы хотите использовать другую модель, измените переменную `OLLAMA_MODEL` в скрипте.*

### 2. Настройка Python окружения
Рекомендуется использовать виртуальное окружение.

```bash
# Создаем папку проекта и переходим в нее, например
mkdir -p ~/Scripts/audio-journal
cd ~/Scripts/audio-journal

# Устанавливаем пакетный менеджер питона
curl -LsSf https://astral.sh/uv/install.sh | sh

# Создаем venv
uv venv --python 3.12

# Устанавливаем зависимости
uv pip install -r requirements.txt
```

### 3. Настройка путей (ОБЯЗАТЕЛЬНО)
Откройте файл `config.py` и отредактируйте под вашу структуру папок:

```python
# Путь к хранилищу аудио (куда переносить обработанные файлы)
LONG_TERM_STORAGE = os.path.join(HOME, "Ваша/Папка/Для/Аудио")

# Путь к корню вашего Obsidian Vault
OBSIDIAN_VAULT_ROOT = os.path.join(HOME, "Obsidian/ВашVault")

# Имя папки-ссылки (см. пункт 4), можно использовать такое же
OBSIDIAN_AUDIO_LINK_NAME = "AudioLinks"
```

### 4. Создание Symlink для Obsidian
Чтобы Obsidian мог проигрывать аудиофайлы, которые лежат вне его папки (в архиве), нужно создать символическую ссылку.

В терминале выполните:
```bash
# Синтаксис: ln -s "Путь_к_Архиву" "Путь_внутри_Obsidian/ИмяСсылки"

ln -s ~/Ваша/Папка/Для/Аудио ~/Obsidian/ВашVault/AudioLinks
```
*Убедитесь, что имя ссылки (`AudioLinks`) совпадает с переменной `OBSIDIAN_AUDIO_LINK_NAME` в скрипте.*

### 5. Доступ к Диску (Full Disk Access)
Папка голосовых заметок (`~/Library/Group Containers/...`) защищена системой.
1. Откройте **Системные настройки** -> **Конфиденциальность и безопасность** -> **Доступ к диску**.
2. Добавьте туда **Terminal** (или iTerm) и **Python** (если запускаете бинарник напрямую), предоставив им полный доступ.

---

## ▶️ Использование

1.  **Запишите голосовую заметку** на iPhone.
2.  Дождитесь, пока она синхронизируется на Mac (обычно 10-30 секунд).
3.  Запустите скрипт:
    ```bash
    # Из папки проекта
    source venv/bin/activate
    uv run transcriber.py
    ```
4.  Скрипт:
    *   Найден новые `.m4a` файлы.
    *   Запустит Ollama (если не запущена).
    *   Транскрибирует и проанализирует.
    *   Создаст `.md` файл в Obsidian.
    *   Переместит аудио в архив.
    *   Сделает `git push` вашего Vault.
    *   Пришлет уведомление macOS.

---

## 🛠️ Решение проблем

*   **`Папка Диктофона не найдена`**: Проверьте путь `VOICE_MEMOS_DIR`. Apple иногда меняет пути в новых версиях macOS, но текущий (`Group Containers/group.com.apple.VoiceMemos.shared/Recordings`) актуален для Sonoma/Sequoia.
*   **Ошибки доступа (`Permission denied`)**: Проверьте Full Disk Access.
*   **Скрипт "зависает" на `Whisper слушает`**: Первая загрузка модели Whisper может занять время. Если Mac старый, смените `WHISPER_SIZE` на `"medium"` или `"small"`.
*   **Obsidian не видит аудио**: Проверьте, что симлинк (`ln -s`) создан корректно и ведет в существующую папку.

---

# 🧠 Мудрый Бро - Анализатор Дневниковых Записей

**Мудрый Бро** — это персональный AI-коуч, который анализирует ваши дневниковые записи и находит неочевидные связи между физическим состоянием, действиями и общим ощущением от жизни. Работает на основе дневников, созданных `Audio Journal Transcriber`.

## 🚀 Установка и Настройка

Выполните шаги [1](#1-установка-ollama-и-модели), [2](#2-настройка-python-окружения) и [3](#3-настройка-путей-обязательно), указанные для Audio Journal Transcriber.

### ✨ Возможности

*   **Еженедельный ретроспективный анализ:** Поиск связей между сном, болями, действиями и оценкой дня
*   **Детектор выгорания:** Выявление признаков хронического стресса и выгорания
*   **Поиск рецепта успеха:** Анализ пиковой продуктивности и паттернов успешных дней
*   **Лингвистический анализ подсознания:** Психолингвистический разбор речи, убеждений и локуса контроля
*   **Лаборатория:** Поиск скрытых паттернов и предложение новых категорий для отслеживания

### 🚀 Использование

```bash
# Показать доступные анализы
uv run wisdom_bro.py

# Запустить конкретный анализ
uv run wisdom_bro.py weekly_retro

# Запустить все анализы
uv run wisdom_bro.py all
```

### 📊 Доступные сценарии

| Ключ | Название | Период |
|------|----------|--------|
| `weekly_retro` | Еженедельный ретроспективный анализ | 7 дней |
| `health_check` | Детектор выгорания | 15 дней |
| `success_recipe` | Поиск рецепта успеха | 30 дней |
| `linguistic_analysis` | Лингвистический анализ подсознания | 30 дней |
| `laboratory` | Лаборатория (поиск новых паттернов) | 14 дней |

### 🔧 Особенности

*   **Локальный ИИ:** Использует Ollama (рекомендуется `qwen3:8b` или другая модель)
*   **Умная фильтрация:** Для лингвистического анализа и лаборатории извлекаются только транскрипты (без метаданных)
*   **Результаты сохраняются:** Каждый анализ сохраняется с промптом для истории и аудита
*   **Автоматическое открытие:** Результаты автоматически открываются в текстовом редакторе

---

## 📄 Лицензия
MIT. Используйте и модифицируйте под себя!

## 📨 Telegram уведомления

Скрипт может дополнительно отправлять уведомления в ваш личный Telegram-бот. Для этого используйте переменные окружения (токен не должен попадать в репозиторий).

- **Переменные окружения**: `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`.
- Файл `.env` уже добавлен в `.gitignore`. В репозитории есть пример `.env.example`.
