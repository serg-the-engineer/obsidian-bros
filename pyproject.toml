[project]
name = "obsidian-bros"
version = "0.1.0"
requires-python = ">=3.10"

[tool.black]
line-length = 88
target-version = ["py312"]

[tool.isort]
profile = "black"

[tool.ruff]
line-length = 88
exclude = [".venv", "venv", "node_modules", "__pycache__"]
